apiVersion: v1
kind: Service
metadata:
  name: playground
  labels:
    app: playground
spec:
  type: NodePort
  selector:
    app: playground
  ports:
    - port: 8888
      nodePort: 30888
      targetPort: 8888
      protocol: TCP